<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Flight_ReservationGui.Pages.TicketsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Grid Padding="32,24,32,32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon Glyph="&#xEB54;" FontSize="28" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
            <TextBlock Text="Ticket Management" FontSize="28" FontWeight="Bold" VerticalAlignment="Center" />
        </StackPanel>

        <InfoBar x:Name="StatusInfoBar" Grid.Row="1" IsOpen="False" />

        <!-- Input Form -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="24" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <StackPanel Spacing="16">
                <TextBlock Text="Ticket Details" FontSize="18" FontWeight="SemiBold" />
                <Grid ColumnSpacing="16" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TxtTicketId" Header="Ticket ID" PlaceholderText="Auto-generated"
                             IsEnabled="False" Grid.Column="0" />
                    <TextBox x:Name="TxtTicketPrice" Header="Ticket Price *" PlaceholderText="e.g. 250.00"
                             Grid.Column="1" />
                    <ComboBox x:Name="CmbBooking" Header="Booking *" PlaceholderText="Select booking"
                              Grid.Column="2" HorizontalAlignment="Stretch" />
                    <ComboBox x:Name="CmbTripType" Header="Trip Type" PlaceholderText="Select type"
                              Grid.Column="3" HorizontalAlignment="Stretch">
                        <ComboBoxItem Content="One Way" />
                        <ComboBoxItem Content="Round Trip" />
                        <ComboBoxItem Content="Multi City" />
                    </ComboBox>
                    <ComboBox x:Name="CmbAircraft" Header="Aircraft" PlaceholderText="Select"
                              DisplayMemberPath="Model" Grid.Column="4" HorizontalAlignment="Stretch"
                              SelectionChanged="CmbAircraft_SelectionChanged" />
                    <ComboBox x:Name="CmbSeat" Header="Available Seat *" PlaceholderText="Select seat"
                              Grid.Column="5" HorizontalAlignment="Stretch" />
                </Grid>

                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="🔄 Refresh" Click="Btn_Refresh_Click" />
                    <Button Content="✖ Clear" Click="Btn_Clear_Click" />
                    <Button x:Name="BtnAdd" Content="➕ Add Ticket" Style="{StaticResource AccentButtonStyle}" Click="Btn_Add_Click" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Tabbed data area: Existing Tickets / Available Seats -->
        <Pivot Grid.Row="3">
            <PivotItem Header="📋 Existing Tickets">
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                        Margin="0,8,0,0">
                    <ListView x:Name="TicketsListView" SelectionMode="Single">
                        <ListView.HeaderTemplate>
                            <DataTemplate>
                                <Grid Padding="8,12" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="100" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Ticket ID" FontWeight="Bold" />
                                    <TextBlock Grid.Column="1" Text="Price" FontWeight="Bold" />
                                    <TextBlock Grid.Column="2" Text="Booking ID" FontWeight="Bold" />
                                    <TextBlock Grid.Column="3" Text="Trip ID" FontWeight="Bold" />
                                    <TextBlock Grid.Column="4" Text="Aircraft ID" FontWeight="Bold" />
                                    <TextBlock Grid.Column="5" Text="Seat #" FontWeight="Bold" />
                                </Grid>
                            </DataTemplate>
                        </ListView.HeaderTemplate>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="100" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding TicketId}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding TicketPrice}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{Binding BookingId}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="3" Text="{Binding TripId}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="4" Text="{Binding AircraftID}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="5" Text="{Binding SeatNumber}" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </PivotItem>

            <PivotItem Header="💺 Available Seats">
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                        Margin="0,8,0,0">
                    <ListView x:Name="SeatsListView" SelectionMode="Single"
                              SelectionChanged="SeatsListView_SelectionChanged">
                        <ListView.HeaderTemplate>
                            <DataTemplate>
                                <Grid Padding="8,12" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Aircraft ID" FontWeight="Bold" />
                                    <TextBlock Grid.Column="1" Text="Seat Number" FontWeight="Bold" />
                                    <TextBlock Grid.Column="2" Text="Class Type" FontWeight="Bold" />
                                </Grid>
                            </DataTemplate>
                        </ListView.HeaderTemplate>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding AircraftID}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding SeatNumber}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{Binding ClassType}" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
