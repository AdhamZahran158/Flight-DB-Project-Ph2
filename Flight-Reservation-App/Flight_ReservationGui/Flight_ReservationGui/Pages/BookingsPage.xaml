<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Flight_ReservationGui.Pages.BookingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Grid Padding="32,24,32,32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon Glyph="&#xE7BC;" FontSize="28" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
            <TextBlock Text="Booking Management" FontSize="28" FontWeight="Bold" VerticalAlignment="Center" />
        </StackPanel>

        <InfoBar x:Name="StatusInfoBar" Grid.Row="1" IsOpen="False" />

        <!-- Input Form -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="24" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <StackPanel Spacing="16">
                <TextBlock Text="Booking Details" FontSize="18" FontWeight="SemiBold" />
                <Grid ColumnSpacing="16" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TxtBookingId" Header="Booking ID" PlaceholderText="Auto-generated"
                             IsEnabled="False" Grid.Column="0" />
                    <ComboBox x:Name="CmbBookingStatus" Header="Booking Status *" PlaceholderText="Select"
                              Grid.Column="1" HorizontalAlignment="Stretch">
                        <ComboBoxItem Content="Confirmed" />
                        <ComboBoxItem Content="Pending" />
                        <ComboBoxItem Content="Cancelled" />
                    </ComboBox>
                    <TextBox x:Name="TxtTotalPrice" Header="Total Price *" PlaceholderText="e.g. 1500.00"
                             Grid.Column="2" />
                    <ComboBox x:Name="CmbPassport" Header="Passenger (Passport) *" PlaceholderText="Select passenger"
                              Grid.Column="3" HorizontalAlignment="Stretch" />
                </Grid>

                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="🔄 Refresh" Click="Btn_Refresh_Click" />
                    <Button Content="✖ Clear" Click="Btn_Clear_Click" />
                    <Button x:Name="BtnAdd" Content="➕ Add Booking" Style="{StaticResource AccentButtonStyle}" Click="Btn_Add_Click" />
                    <Button x:Name="BtnUpdate" Content="✏️ Update Status" Click="Btn_Update_Click" />
                    <Button x:Name="BtnDelete" Content="🗑️ Delete" Click="Btn_Delete_Click" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Data List -->
        <Border Grid.Row="3" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <ListView x:Name="BookingsListView" SelectionChanged="BookingsListView_SelectionChanged"
                      SelectionMode="Single">
                <ListView.HeaderTemplate>
                    <DataTemplate>
                        <Grid Padding="8,12" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="ID" FontWeight="Bold" />
                            <TextBlock Grid.Column="1" Text="Date" FontWeight="Bold" />
                            <TextBlock Grid.Column="2" Text="Status" FontWeight="Bold" />
                            <TextBlock Grid.Column="3" Text="Total Price" FontWeight="Bold" />
                            <TextBlock Grid.Column="4" Text="Passport #" FontWeight="Bold" />
                        </Grid>
                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding BookingId}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{Binding BookingDate}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{Binding BookingStatus}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="3" Text="{Binding TotalPrice}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="4" Text="{Binding PassportNum}" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
